---
/**
 * DISABLED: Pure blog mode
 * This component is disabled while the site operates as a pure content blog.
 * To restore product functionality, uncomment the original code below.
 */
import type { FeaturedProduct } from '../data/home';
interface Props {
  id?: string;
  title?: string;
  icon?: string;
  products?: FeaturedProduct[];
  alternate?: boolean;
  showViewAll?: boolean;
  totalCount?: number;
}
---

<!-- Disabled: pure blog mode - ProductCategorySection -->

<!--
ORIGINAL CODE - COMMENTED OUT FOR RESTORATION:
See git history or backup for full ProductCategorySection implementation
with category headers, product cards grid, and View All links.
-->
