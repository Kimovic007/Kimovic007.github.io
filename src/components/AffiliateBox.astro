---
/**
 * DISABLED: Pure blog mode
 * This component is disabled while the site operates as a pure content blog.
 * To restore affiliate functionality, uncomment the original code below.
 * See docs/archived/PRODUCT_ID_SYSTEM.md for restoration instructions.
 */
interface Props {
  product?: any;
}
---

<!-- Disabled: pure blog mode - AffiliateBox -->

<!--
ORIGINAL CODE - COMMENTED OUT FOR RESTORATION:

interface Props {
  product: {
    name: string;
    image: string;
    price: string;
    originalPrice?: string;
    rating?: number;
    link: string;
    description?: string;
    discount?: string;
    dealEndTime?: string;
  };
}

const { product } = Astro.props;

<div class="bg-gradient-to-br from-slate-800 to-slate-900 rounded-2xl p-6 shadow-xl relative overflow-hidden">
  ... original component code ...
</div>
-->
