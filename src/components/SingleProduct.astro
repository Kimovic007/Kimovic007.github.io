---
/**
 * DISABLED: Pure blog mode
 * This component is disabled while the site operates as a pure content blog.
 * To restore product functionality, uncomment the original code below.
 */
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<!-- Disabled: pure blog mode - SingleProduct component for id="{id}" -->
<!-- TODO: Add editorial product description for this product -->

<!--
ORIGINAL CODE - COMMENTED OUT FOR RESTORATION:

import ProductCard from './ProductCard.astro';
import { productCategories } from '../data/products';

interface Props {
  id: string;
}

const { id } = Astro.props;
const product = productCategories
  .flatMap(cat => cat.products)
  .find(p => p.id === id);

<div class="my-8 flex justify-center">
  <div class="w-full max-w-3xl">
    {product ? (
      <ProductCard ... />
    ) : (
      <div>Product Not Found</div>
    )}
  </div>
</div>
-->
